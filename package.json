{
  "name": "grainedge-full",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:all": "cd grainedge-quote && npm install && npm run build && cd .. && node build.mjs"
  }
}
